---
import ThemeToggle from "./ThemeToggle.astro";

const navItems = [
   { title: "Proyectos", label: "proyectos", url: "/#proyectos" },
   { title: "Sobre mí", label: "sobre-mi", url: "/#sobre-mi" },
   { title: "Formación", label: "formacion", url: "/#formacion" },
   {
      title: "Contacto",
      label: "contacto",
      url: "mailto:santiago.goncalvez.dev@gmail.com",
   },
];
---

<header
   id="header"
   class="fixed top-0 inset-x-0 z-50
         transition-colors duration-300
         border-b border-transparent"
>
   <div class="mx-auto max-w-6xl sm:px-5 px-2">
      <nav
         class="flex items-center justify-between py-4
             w-full mx-auto lg:w-[900px]"
      >
         <a
            class="nav-link text-xs sm:text-sm font-bold tracking-tight"
            aria-label="inicio"
            href="#top"
         >
            <img
               src="/favicon.svg"
               alt="Logo de Santiago Goncalvez"
               class="sm:size-8 size-0"
            />
         </a>

         <div class="flex items-center sm:gap-2 gap-0">
            {
               navItems.map((link) => (
                  <a
                     class="nav-link relative
   sm:px-3.5 px-2 py-0.5 rounded-full
   text-sm sm:text-base font-medium tracking-tight
   transition-colors duration-300"
                     aria-label={link.label}
                     href={link.url}
                  >
                     {link.title}
                  </a>
               ))
            }
         </div>

         <ThemeToggle />
      </nav>
   </div>
</header>

<script>
   const initNavScroll = () => {
      const header = document.getElementById("header") as HTMLElement | null;
      if (!header) return;

      const sections = document.querySelectorAll<HTMLElement>("section[id]");
      const links = document.querySelectorAll<HTMLElement>(".nav-link");
      const root = document.documentElement;

      const OFFSET: number = 120;

      const normalize = (str: string): string =>
         str.normalize("NFD").replace(/[\u0300-\u036f]/g, "");

      const applyNavTextColor = (): void => {
         const isDark: boolean = root.classList.contains("dark");

         links.forEach((link) => {
            link.classList.remove("text-title", "text-bg-base");

            if (isDark) {
               link.classList.add("text-title");
            } else {
               link.classList.add("text-bg-base");
            }
         });
      };

      const resetHeader = (): void => {
         header.classList.remove(
            "bg-title",
            "bg-bg-surface",
            "border-black/10",
            "border-white/10",
         );

         // 🔑 estado inicial real
         header.classList.add("border-transparent");
      };

      const applyHeaderState = (): void => {
         const isDark: boolean = root.classList.contains("dark");
         const y: number = window.scrollY;

         // 🔥 Reset total
         resetHeader();

         // 🟢 Solo aparece fondo + borde después del primer scroll
         if (y > 0) {
            header.classList.remove("border-transparent");

            if (isDark) {
               header.classList.add("bg-bg-surface", "border-white/10");
            } else {
               header.classList.add("bg-title", "border-black/10");
            }
         }
      };

      const setActiveLink = (id: string): void => {
         const isDark = root.classList.contains("dark");

         links.forEach((link) => {
            const label = normalize(link.getAttribute("aria-label") ?? "");

            // 🔥 Reset TOTAL
            link.classList.remove(
               "bg-bg-base",
               "bg-title",
               "text-title",
               "text-bg-base",
            );

            // 🟡 Estado base (no activo)
            if (isDark) {
               link.classList.add("text-title");
            } else {
               link.classList.add("text-bg-base");
            }

            // 🟢 Activo
            if (label === id && id !== "") {
               link.classList.remove("text-title", "text-bg-base");

               if (isDark) {
                  // DARK
                  link.classList.add("bg-title", "text-bg-base");
               } else {
                  // LIGHT
                  link.classList.add("bg-bg-base", "text-title");
               }
            }
         });
      };

      const updateActiveSection = (): void => {
         const y: number = window.scrollY;

         if (y < 100) {
            setActiveLink("");
            return;
         }

         let current: string = "";

         sections.forEach((section) => {
            if (y + OFFSET >= section.offsetTop) {
               current = normalize(section.id);
            }
         });

         setActiveLink(current);
      };

      const update = (): void => {
         applyHeaderState();
         updateActiveSection();
      };

      // Scroll
      window.addEventListener("scroll", update, { passive: true });

      // Cambio de theme
      const observer = new MutationObserver(() => update());
      observer.observe(root, {
         attributes: true,
         attributeFilter: ["class"],
      });

      update();
   };

   document.addEventListener("DOMContentLoaded", initNavScroll);
   document.addEventListener("astro:after-swap", initNavScroll);
</script>
