<section
   class:list={[
      "w-full max-w-screen-2xl flex flex-col sm:gap-8 gap-8 md:scroll-mt-28 scroll-mt-24",
      // animateOnScroll && "opacity-0 translate-y-6",
      // "no-mobile-motion",
      Astro.props.class,
   ]}
   id={Astro.props.id}
   {...Astro.props}
>
   <slot />
</section>
