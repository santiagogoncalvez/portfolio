<section
   class:list={[
      "w-full mx-auto lg:max-w-5xl flex flex-col gap-8 scroll-mt-24",
      // animateOnScroll && "opacity-0 translate-y-6",
      // "no-mobile-motion",
      Astro.props.class,
   ]}
   id={Astro.props.id}
   {...Astro.props}
>
   <slot />
</section>
