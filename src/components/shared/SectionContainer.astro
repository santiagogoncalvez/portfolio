---
// const { animateOnScroll = true, disableOnMobile = false } = Astro.props;
---

<section
   class:list={[
      "w-full mx-auto lg:w-[900px] flex flex-col gap-8 scroll-mt-22",
      // animateOnScroll && "opacity-0 translate-y-6",
      // "no-mobile-motion",
      Astro.props.class,
   ]}
   id={Astro.props.id}
   {...Astro.props}
>
   <slot />
</section>
